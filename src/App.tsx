import React from 'react';
import {SafeAreaView} from 'react-native';
import {HelloScreen} from 'screens';

const App: React.FC = () => (
  <SafeAreaView>
    <HelloScreen />
  </SafeAreaView>
);

export default App;
